# Remove Broad Fallback Links

If a skill is already linked under a more specific nested category such as `Retrieval/Traversal`, a rebuild pass can remove broader ancestor links like `Retrieval`.

This makes the category tree cleaner and reduces retrieval noise.
